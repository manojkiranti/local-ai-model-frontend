/**
 * Product-facing naming, in one place so the app can be renamed without hunting
 * through components. (The backend project is still called the "Local LLM
 * Gateway" — that's an internal deployment detail, not user-facing copy.)
 */
export const APP_NAME = 'NIC ASIA AI Workspace'

/** The two-tone wordmark split — "NIC ASIA" in brand red, "AI Workspace" in foreground. */
export const APP_NAME_PRIMARY = 'NIC ASIA'
export const APP_NAME_SECONDARY = 'AI Workspace'

/** One-line description shown under the name on the auth screens. */
export const APP_TAGLINE = 'Think Smart. Work Better. Do More.'
